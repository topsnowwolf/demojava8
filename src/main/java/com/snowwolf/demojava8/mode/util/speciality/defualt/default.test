引入默认方法的好处：
在java8以前，一个接口新加了一个新的方法，实现它的类必须重写才不会报异常。
而java8以后，新加一个方法，方法为默认方法，实现它的类必须要重写。同时默认方法可以写实现内容。
这样就大大减少了，底层组件因业务或功能改动，需要升取级时，避免和它相关的组件造成影响。
举个例子：
一个基础组件A，一个业务组件B，业务组件是基于基础组件A开发的，现在业务和功能有了很多的变化，
基础组件A已经不能满足当前业务组件的需求，必须扩展。但是有些项目却不需要，此时为了不造成更多
的改动，就在基础组件A对应的接口中新加默认方法。从而避免，由于基础组件A的改动，导致所有依赖
于它的出现问题。
这样基础组件的扩展和升级就不会导致，业务组件的报错！


使用默认方法要注意的事项：


默认方法的特点：
1.接口包含的默认方法，在它实现的类中可以不提供实现。
2.默认方法前使用defualt修饰。


默认方法的继承机制：
当一个类的函数签名同多个地方（例如继承类实现接口）的相同。最终执行的规则如下：
1.类的方法优先级最高。它高于任何声明为默认的方法。
2.当1无法判断。子接口的优先级更高。例如B继承A，B中高于A的。
3.1,2都无法满足。就必须通过显示覆盖或调用期望的方法处理。